# Copy this file to .env.local for local development with a non-default backend URL.
# In production (Vercel), set REACT_APP_API_URL as an Environment Variable in the dashboard.

REACT_APP_API_URL=http://localhost:8080/api
